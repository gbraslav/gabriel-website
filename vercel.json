{
  "buildCommand": "pnpm install && pnpm run build && cd stephanie/stephanie-website && npm install && npm run build && cd ../.. && mkdir -p dist/stephanie && cp -r stephanie/stephanie-website/build/. dist/stephanie/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/stephanie",
      "destination": "/stephanie/index.html"
    }
  ]
}

